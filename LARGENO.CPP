#include<stdio.h>
#include<conio.h>
int main()
{
clrscr();
int a,b;
printf("Enter two no's :");
scanf("%d%d",&a,&b);
if (a>b)
{
printf("%d is the greatest no.",a);
}
else
{
printf("%d is the greatest no.",b);
}


getch ();
return 0;
}
